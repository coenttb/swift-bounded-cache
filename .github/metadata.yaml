description: "Cache<Key, Value> — a thread-safe compute-if-absent async cache; concurrent callers for the same key coalesce onto a single in-flight computation."
topics:
  - molecules
  - cache
homepage: "https://swift-institute.org"
readme:
  family: E
